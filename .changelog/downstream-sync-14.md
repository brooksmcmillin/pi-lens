### Fixed

- Reconcile upstream governance inventories with downstream skill activation, private claim receipts, and external probe logs (refs #14). Keep exact glossary counts and explicitly admit real Git probe checks into the serialized test lane.
